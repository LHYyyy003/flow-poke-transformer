# Physics bias 多场景泛化与 loss 平台诊断

## 结论

本次结果支持两个不同层面的判断：

1. 原始“仅偏置”和全程长时序训练都学到了跨场景有害的 attention bias。长时序模型从
   500 步继续到 1000/1500 步时，验证 rollout 持续恶化，属于明确的继续训练退化。
2. 碰撞平滑 500 步模型是目前唯一在五场景宏平均上优于原模型的版本，但收益并不均匀；
   继续训练到 1500 步后，mean EPE 和碰撞后窗口明显反弹。因此应以 500 步为当前候选，
   不能根据训练 loss 或单个正碰场景选择 1500 步模型。

训练 loss 基本不下降并非单纯因为下降趋势被波动完全遮住。501–1500 步数据中，原始 loss
的批次波动确实很大，但 100 步窗口的末段均值还比首段高 `0.95%`；碰撞 loss 高 `0.37%`，
非碰撞 loss 高 `3.08%`。梯度始终非零、参数也发生变化，说明不是断图或优化器没有工作；
更主要的问题是随机 flow-matching 单步目标已进入平台，且它与 48 步自回归 rollout 指标不一致。

## 验证协议

- 五个确定性场景：正碰、斜碰、擦碰、无碰撞、仅撞墙；
- 每个场景 16 个球、2 个已知状态、48 个预测状态；
- 每个场景在所有模型间使用相同 RF 采样种子；
- 同时统计宏平均、最坏场景、最终步、球–球碰撞后 10 步窗口及非碰撞区域 EPE；
- `bias_only_1000` 使用 11 维关系输入和短历史；
- `long_history_*` 使用 14 维关系输入和全程八状态长历史；
- `collision_smooth_500` 是已预乘 strength `0.25` 的 checkpoint，评估时没有重复缩放。

原始数据见：

- `outputs/physics_multiscene_validation/metrics.json`
- `outputs/physics_multiscene_validation/short_ablation_metrics.json`
- `outputs/physics_multiscene_validation/smooth_metrics.json`
- `outputs/physics_multiscene_validation/loss_analysis.json`

## 多场景结果

| 模型 | 宏平均 mean EPE | 宏 P95 | 宏 final EPE | 碰撞后窗口 | 非碰撞区域 | 最坏场景 mean |
|---|---:|---:|---:|---:|---:|---:|
| 原模型 | 0.09482 | 0.65178 | 0.21465 | 0.28985 | 0.09140 | 0.10178 |
| 仅偏置 @1000 | 0.14852 | 0.96320 | 0.36205 | 0.69943 | 0.13955 | 0.24016 |
| 全程长时序 @500 | 0.16165 | 1.14377 | 0.40087 | 0.63323 | 0.15416 | 0.26663 |
| 全程长时序 @1000 | 0.21975 | 1.44218 | 0.54254 | 1.08861 | 0.20577 | 0.37315 |
| 全程长时序 @1500 | 0.23212 | 1.36980 | 0.58439 | 0.99653 | 0.21972 | 0.30667 |
| 碰撞平滑 @500 | **0.08219** | 0.62571 | **0.18315** | 0.36124 | **0.07745** | 0.11933 |
| 碰撞平滑 @1500 | 0.10181 | **0.58735** | 0.21180 | 0.51076 | 0.09477 | 0.12962 |

单位均为像素。碰撞平滑 500 相对原模型的宏 mean、P95、final 和非碰撞 EPE 分别改善
`13.3% / 4.0% / 14.7% / 15.3%`，但碰撞后窗口恶化 `24.6%`，最坏场景 mean 恶化
`17.2%`。它改善斜碰、无碰撞和撞墙，但正碰与擦碰变差，所以还不能称为全面泛化提升。

碰撞平滑从 500 继续到 1500 后：

- 宏 mean EPE 恶化 `23.9%`；
- 宏 final EPE 恶化 `15.6%`；
- 碰撞后窗口恶化 `41.4%`；
- 非碰撞区域恶化 `22.4%`；
- 宏 P95 改善 `6.1%`。

P95 与其他指标方向不同，说明训练不是简单地让所有预测同时变差，而是在场景和误差分位间重新
分配收益。1500 步只在无碰撞场景继续改善；相对 500 步，正碰、斜碰、擦碰和撞墙 mean EPE
分别恶化 `21.3% / 60.2% / 3.5% / 150.8%`。

## 长历史是否是直接原因

把同一个长时序 checkpoint 的运行时模式从 `long` 切换为 `short`，保持 MLP 和 scale 权重不变：

| checkpoint | long 宏 mean | short 宏 mean | long 相对 short |
|---|---:|---:|---:|
| 500 | 0.16165 | 0.13793 | +17.2% |
| 1000 | 0.21975 | 0.23856 | -7.9% |
| 1500 | 0.23212 | 0.19971 | +16.2% |

长历史在 500/1500 步放大退化，在 1000 步却略有缓解。因此不能把全部退化归因于“使用了八状态
历史”；更稳定的解释是关系 MLP/scale 学到了跨场景有害的 bias，而长历史特征与这些权重交互后，
会因 checkpoint 不同而放大或减轻问题。11 维仅偏置模型相对原模型宏 mean 恶化 `56.6%`，也支持
“有害 bias 早于长历史出现”。

## 为什么训练 loss 基本不下降

可恢复的 TensorBoard 事件覆盖碰撞平滑模型从全局 501 到 1500 的全部 1000 步。早期旧训练的
TensorBoard 事件已不在磁盘，因此以下精确曲线结论只针对这次 500→1500 续训。

| 指标 | 前 100 步均值 | 后 100 步均值 | 变化 | 全程标准差 / 均值 |
|---|---:|---:|---:|---:|
| 总 loss | 0.07702 | 0.07775 | +0.95% | 31.5% |
| collision loss | 0.11164 | 0.11205 | +0.37% | 43.0% |
| non-collision loss | 0.05675 | 0.05849 | +3.08% | 35.3% |
| 单步 EPE | 9.468e-5 | 9.355e-5 | -1.20% | 18.3% |
| collision EPE | 1.072e-4 | 1.078e-4 | +0.59% | 29.7% |

原因按证据强弱排序：

1. **loss 确实处于平台，而不只是下降被噪声遮住。** 波动远大于首尾差异，但首尾窗口、碰撞与
   非碰撞分组都没有持续下降；100 步滑动均值中间下降后又反弹。
2. **每步 loss 本身是高方差随机量。** RF loss 会为每个 token 重新采样时间 `t` 和高斯噪声 `z`；
   在线数据又每批生成新场景，所以原始训练 loss 不应期待平滑单调。
3. **批次难度和碰撞组成解释了相当一部分波动。** 总 loss 与真实 flow 幅值的相关系数为 `0.513`，
   与碰撞 token 比例为 `0.453`，与 collision loss 为 `0.774`。平均只有 `16.5%` token 落在碰撞
   窗口；按 `3x` 权重折算后约占有效 loss 权重的 `37%`，因此总 loss 仍受大量非碰撞 token 和
   场景速度分布影响。
4. **不是梯度消失或模块未连接。** grad norm 均值为 `0.0120`，末 100 步还比首 100 步高 `6.4%`；
   500→1500 期间 layer/head scales 的相对变化为 `2.4%`，MLP 输出层权重变化为 `10.4%`。
5. **后半程学习率已过低。** 学习率在 step 600 达到 `1e-5`，之后余弦衰减；末 100 步均值只有
   `1.09e-7`，最后一步约 `1e-8`。后段即使有梯度，也几乎无法形成大的参数修正。
6. **优化目标与最终指标错位。** 训练优化带随机噪声的单步 flow-matching MSE；最终关心的是预测
   位置反复写回历史后的 48 步 rollout。单步 EPE 仅改善 `1.2%`，但同一时期多场景 rollout mean
   恶化 `23.9%`、碰撞后窗口恶化 `41.4%`，说明更低或近似不变的单步目标不能保证长程稳定性。

## 建议

1. 暂停继续训练 1500 步模型，以碰撞平滑 500 作为下一轮起点，但不将其标成全面优于基线。
2. 每 100–250 步运行同一固定多场景验证集，按“宏 mean + 碰撞后窗口 + 最坏场景”联合早停，
   不再用训练 loss 选 checkpoint。
3. 验证 loss 固定数据、固定 RF `t/z` 或对多个噪声样本求平均，把数据/噪声方差与真实趋势分开。
4. 下一轮优先加入短程 scheduled-sampling 或截断 rollout consistency loss，让训练直接看到预测历史
   噪声；仅继续提高碰撞权重缺乏依据，因为 collision loss 与 collision EPE 当前都没有改善。
5. 对 bias 增加幅值正则或更保守的可学习门控，并分别约束球–球、无碰撞和墙碰撞场景，避免关系
   bias 在模型本来已经准确的场景上产生不必要修正。

## 局限

五个场景覆盖了关键碰撞类型，但每类只有一个手工固定场景、一个 RF rollout seed；它足以揭示当前
checkpoint 的强烈方向性退化，但不是最终统计显著性结论。下一阶段应扩展为每类多个初始位置、速度、
角度和种子，并报告置信区间。
