# Model artifacts

Large checkpoints are intentionally excluded from Git and stored on the AutoDL data disk.

Artifact root:

```text
/root/autodl-tmp/flow-poke-transformer-repro/models
```

The optimized inference profile uses collision-smooth kinematics with distance `0.04`,
temperature `0.008`, and physics strength `0.25`. See the artifact root's
`SHA256SUMS` and `ARTIFACTS.txt` for exact checkpoint paths and checksums.
