# Physics Bias 诊断实验

目标：区分以下三类失败，而不是继续依靠训练 loss 的单条曲线猜测。

1. 模块没有真正影响模型（接线、mask、尺度或梯度问题）。
2. 模块能影响输出，但优化器无法学会目标（优化或容量问题）。
3. 模块能学习训练目标，但训练目标/数据分布与碰撞 rollout 指标不一致。

## 固定条件

- 固定随机种子、原始 `myriad_billiard.pt`、DINO 权重和 eager attention。
- 固定一组训练场景和一组从未参与更新的验证场景。
- 验证集至少包含：正碰、斜碰、擦碰、不碰撞、撞墙五类，每类 32 个场景。
- 除总体 loss 外，始终记录碰撞参与球 EPE、非碰撞球 EPE、碰撞后 10/25/48 步 EPE。
- 每个实验至少运行 3 个随机种子，报告均值和标准差。

## 实验 A：模块接线与梯度测试

在同一个未训练的 physics 模型上执行四次完全相同的前向传播：

| 条件 | 预期结果 |
|---|---|
| physics bias 为 0 | 输出应与原始模型在数值误差内一致 |
| 人工注入较大的常数 bias | 注意力或最终输出必须产生可测变化 |
| 正常 bias，执行一次 backward | 最后一层 MLP 的梯度必须非零且有限 |
| 打乱球 ID/历史时间 | 输出变化应证明物理元数据确实进入计算图 |

判定：零 bias 不等价、强制 bias 仍无输出变化，或梯度恒为 0，说明模块实现/接线有问题；此时不应继续长时间训练。

## 实验 B：单批次过拟合测试

选择 8–16 个碰撞丰富的固定场景，只训练 physics generator，不使用 warmup，以 `1e-4` 和 `3e-4` 两个学习率各训练 300 步。每一步使用同一批数据和同一 RF 噪声。

同时记录：

- 固定批次 loss；
- 碰撞参与球的 one-step EPE；
- generator 各层梯度范数和参数变化量；
- bias 的 mean/max、饱和率以及每层每 head 的 scale。

判定：若实验 A 通过，但固定批次 loss 和碰撞 EPE 均不能明显下降（建议阈值 30%），则是优化、表达能力或 bias 注入位置的问题，而不是数据泛化问题。

## 实验 C：同一权重的开关消融

对训练 checkpoint 在完全相同的固定场景、采样种子下比较：

1. 原始模型；
2. 训练后的模型，physics bias 开启；
3. 同一个训练后模型，physics bias 关闭；
4. physics bias 开启，但打乱相对速度/closing speed；
5. 逐项移除 surface distance、time-to-closest-approach、approaching。

判定：若关闭 bias 后恢复原始模型精度，而开启后变差，退化可直接归因于新增模块学到的 bias。若打乱物理特征几乎不改变结果，则网络没有利用这些物理量，只学到了通用注意力偏置。

## 实验 D：监督目标与采样分布测试

保持模型结构和初始化不变，仅改变训练数据/损失：

- 对照组：当前所有 800 个 token 等权平均；
- 碰撞采样组：至少 50% batch 含碰撞；
- 碰撞加权组：碰撞参与球及碰撞后窗口权重为 10，其余为 1；
- 两阶段组：先用碰撞加权 loss 训练 physics-only，再以较小学习率解冻最后 2 层。

只有当验证集的碰撞后 rollout EPE 改善且非碰撞 EPE 不显著退化时才视为有效；训练平均 loss 下降不作为单独成功标准。

## 决策顺序

1. A 失败：修模块实现。
2. A 通过、B 失败：调整注入位置、bias 尺度、优化器或模块容量。
3. A/B 通过、C 表明 bias 有害：检查特征定义和因果 mask。
4. A/B/C 通过但验证不改善：执行 D，修正碰撞监督和数据分布。

## 2026-08-05 实验结果

已运行 `scripts/myriad_eval/diagnose_physics_module.py`：

- 零初始化 bias 与关闭模块的最大输出差为 `0`；
- 强制非零 bias 后，冻结基础网络的平均输出变化为 `0.00704`；
- generator 首步梯度范数为 `6.74e-4`，有限且非零；
- 教师—学生固定样本训练 200 步，MSE 从 `8.07e-5` 降至 `2.61e-9`，下降 `99.997%`；
- 真实第 1000 步 checkpoint 的末层权重范数为 `0.5087`，layer/head scale 相对初值的最大变化为 `0.1105`。

同场景开关消融中，训练后 checkpoint 关闭 bias 时与原始模型完全一致；开启 bias 后平均 rollout EPE 增加 `49.5%`。因此实验 A、B 均通过，模块已经接入且可以学习；当前失败属于真实训练数据/目标学出了有害 bias，应优先执行实验 D，而不是继续增加 warmup。

## 第一轮优化

- 数据集根据模拟器返回的球索引，只标记球—球碰撞参与者，不把墙碰撞错误分配给球间关系模块；
- 碰撞参与球及后续 10 步支持可配置 loss 权重，默认采用较温和的 `3x`；
- TensorBoard 分别记录 collision/non-collision loss 和 EPE；
- 固定场景强度扫描结果：scale `1.0 → 0.1057 px`、`0.5 → 0.0663 px`、`0.25 → 0.0788 px`，因此新训练把独立 layer/head scale 初始化为 `0.5`；
- ±0.5 硬截断得到 `0.1153 px`，没有采用；保留 ±1 上限，仅缩小初始强度；
- checkpoint 改为临时文件加原子替换。磁盘写满时删除不完整临时文件、保留上一份有效模型并停止训练。

20 步真实模型短程对照中，`10x` 虽使 collision RF loss 比均匀 loss 下降更快，但没有同步改善采样 EPE；因此该权重不作为默认值。`3x` 用作下一轮多场景验证的实验起点，最终仍必须由固定验证集 rollout EPE 选择，而不能由训练总 loss 选择。
