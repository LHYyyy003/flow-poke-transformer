# 去物理偏置的长时序注意力消融

## 结论

不建议部署本次纯长时序注意力 checkpoint。它在固定的一步流匹配验证上降低了 loss，
但在五类确定性场景的自回归 rollout 上全面劣化，而且训练越久，整体劣化越明显。
因此，物理特征不是原先效果不好的唯一原因；更主要的问题是附加注意力偏置所优化的
一步训练目标，与长期自回归稳定性不一致。

主干模型没有被训练破坏：1500 步 checkpoint 关闭新偏置后，在固定验证的每一个 batch
以及五个 rollout 场景上都与原始基线逐项完全一致。

## 严格消融设计

- 从官方 `myriad_billiard.pt` 基线初始化。
- 冻结 DINO、24 层 Transformer、输出投影和 distribution head；只训练约 5 千个偏置参数。
- 新模块只输入：归一化时间差、时间差绝对值、同轨迹标记、键是否已观测、是否为因果历史、可用历史长度。
- 不输入位置、相对位置、距离、速度、加速度、球半径、碰撞标签、接近速度或 TTC。
- 单元测试要求任意改变位置张量后偏置输出逐位不变。
- 为降低附加模块干预，最大偏置由物理版的 1.0 收紧到 0.25，初始层门控由 0.5 降到 0.25。
- 其余训练条件与物理偏置实验一致：batch 4、碰撞 loss 权重 3、碰撞窗口 10、AdamW、
  峰值学习率 `1e-4`、warmup 200、cosine、1500 optimizer steps。

## 训练完整性

- 训练时间：42 分 43 秒。
- checkpoint：500、1000、1500 步均完整写入，每个 2,611,713,367 字节。
- 1500 个 TensorBoard loss 点完整存在。
- 梯度始终非零，平均 grad norm 0.00735，没有 NaN/Inf 或梯度爆炸。
- 43 个仓库测试全部通过；两步真实 GPU 烟雾训练也成功保存 checkpoint。

训练 loss 的 250 步窗口均值为：

| steps | total loss | collision loss | noncollision loss |
|---:|---:|---:|---:|
| 1-250 | 0.08080 | 0.11840 | 0.05922 |
| 251-500 | 0.07742 | 0.11201 | 0.05730 |
| 501-750 | 0.07600 | 0.11060 | 0.05666 |
| 751-1000 | 0.07308 | 0.10310 | 0.05623 |
| 1001-1250 | 0.07537 | 0.10748 | 0.05742 |
| 1251-1500 | 0.07678 | 0.10789 | 0.05921 |

单步 total loss 的总体均值为 0.07658，标准差为 0.02376（约为均值的 31%）。
这解释了为什么原始训练曲线肉眼看起来“基本不下降”：前 1000 步存在缓慢下降，
但 batch 难度波动远大于短窗口内的优化收益；1000 步后则确实进入平台并轻微回升。

## 固定验证：一步目标改善

协议为固定 8 个 batch、共 32 个样本，并对每个 batch 固定 RF 噪声。

| 模型 | loss | 相对基线 | collision loss | 相对基线 | EPE 相对基线 |
|---|---:|---:|---:|---:|---:|
| 无偏置基线 | 0.064412 | - | 0.087546 | - | - |
| 长时序 500 | 0.062724 | -2.62% | 0.084262 | -3.75% | -0.67% |
| 长时序 1000 | 0.061983 | -3.77% | 0.082581 | -5.67% | -0.86% |
| 长时序 1500 | 0.062043 | -3.68% | 0.082628 | -5.62% | -0.89% |
| 1500 关闭偏置 | 0.064412 | 0.00% | 0.087546 | 0.00% | 0.00% |

固定一步目标在 1000 步最佳，1000 到 1500 没有继续改善。

## 五场景 rollout：长期效果劣化

场景包括正碰、斜碰、擦碰、无碰撞和墙反弹；每个模型使用相同随机种子。

| 模型 | macro mean EPE | 相对基线 | 末帧 EPE 相对基线 | 碰撞窗口 EPE 相对基线 |
|---|---:|---:|---:|---:|
| 无偏置基线 | 0.09482 px | - | - | - |
| 长时序 500 | 0.11175 px | +17.85% | +20.69% | +56.51% |
| 长时序 1000 | 0.12093 px | +27.53% | +29.63% | +107.81% |
| 长时序 1500 | 0.13716 px | +44.65% | +48.03% | +128.38% |
| 1500 关闭偏置 | 0.09482 px | 0.00% | 0.00% | 0.00% |

所以本实验具有典型的目标层面过拟合：一步 teacher-forced 指标改善，但自回归误差累积显著恶化。
这不是有限训练集记忆导致的传统过拟合，因为训练数据来自在线程序生成；它更接近训练目标、
模型介入位置和推理分布之间的错配。

## 与物理偏置消融合并解释

此前 collision-smooth 物理偏置 500 步的总体 macro mean EPE 曾比基线好 13.32%，
但碰撞窗口 EPE 仍差 24.63%；继续到 1500 步后总体 EPE 也比基线差 7.37%，
碰撞窗口差 76.21%。本次去掉全部物理输入后，500 步总体 EPE 已差 17.85%，
1500 步差 44.65%。

这说明：

1. 物理偏置并非天然只会劣化；平滑碰撞门控在短训练阶段提供过一定有用的局部归纳偏置。
2. 但现有物理特征没有真正改善碰撞窗口，继续训练会放大长期误差。
3. 仅保留长时序身份/时间关系更差，说明问题不能简单归因于速度或加速度估计噪声。
4. 当前最可靠的部署选择仍是原始无偏置基线；若继续研究，应直接加入多步 rollout/闭环损失，
   或解冻少量主干层共同适配，而不是继续增加一步偏置训练步数。

## 产物

- 完整指标：`outputs/long_history_attention_ablation/metrics.json`
- 训练目录：`/root/autodl-tmp/flow-poke-transformer-repro/training/long_history_attention_1500/2026-08-06/15-21-32`
- TensorBoard：`/root/tf-logs/flow-poke/billiard-long-history/2026-08-06/15-21-32`
- 推荐部署：`checkpoints/myriad_billiard.pt`（无偏置基线）
- 若仅用于继续研究：500 步 checkpoint 是三个纯长时序模型中 rollout 劣化最小者。
